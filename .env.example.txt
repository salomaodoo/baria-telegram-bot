# Token do Bot do Telegram (obtido do BotFather)
BOT_TOKEN=seu_token_real_aqui
# Chave da API do Groq (para IA)
GROQ_API_KEY=sua_chave_groq_aqui
# Porta (será definida automaticamente pelo Railway/Render)
PORT=5000
# Ambiente
FLASK_ENV=production
